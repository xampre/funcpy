# Funcpy
_provide several functions to manipulate function

## Installation
`pip install funcpy`

## Usage
```python
import funcpy
funcpy.fpow(lambda x: x + 1, 50)(0) # => 50
```

## License
MIT license
